package mineript.code.values;

public enum TokenType {
    NUMBER,
    WORD,
    TEXT,
    BOOLEAN,

    // keyword
    CLASS,
    PRINT,
    PRINTLN,
    IF,
    ELSE,

    WHILE,
    FOR,
    DO,
    BREAK,
    CONTINUE,

    DEF,
    RETURN,

// --------------
    PLUS,
    MINUS,
    STAR,
    SLASH,
    SET,

    EQUALS,
    NOT,
    NOTEQUALS,
    LT,
    LTEQUALS,
    GT,
    GTEQUALS,

    AND,
    ANDAND,
    OR,
    OROR,

    LPAR,
    RPAR,

    LBRACE,
    RBRACE,
    LBRAKE,
    RBRAKE,

    COMMA,
    DOT,

    END
}
